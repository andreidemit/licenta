# CLAUDE.md

Sursa canonică de instrucțiuni pentru asistenții AI este
[`.github/copilot-instructions.md`](.github/copilot-instructions.md).

Acel document descrie:
- cele două stive Python independente (`src/` legacy + `environment/agents/simulation/experiments/` safe navigation);
- aplicația web (FastAPI + React) care expune ambele stive;
- toate comenzile pentru rulare, training, comparații Monte Carlo și teste;
- convențiile arhitecturale și capcanele cunoscute (de ex. cele două clase distincte numite `EpisodeResult`).

Acest fișier există doar pentru asistenții care caută `CLAUDE.md` din obișnuință. Nu duplicați conținutul aici — actualizați doar documentul canonic.
